import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Frontier Pulse",
  description:
    "Real-time civilization health monitor for EVE Frontier. Combining universe-scale activity visualization with behavioral trust intelligence to reveal whether civilization is truly thriving.",
  keywords: [
    "EVE Frontier",
    "Sui blockchain",
    "civilization health",
    "trust intelligence",
    "Web3",
    "hackathon",
  ],
  icons: {
    icon: "/logo/logo.png",
    apple: "/logo/logo.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
